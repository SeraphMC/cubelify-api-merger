name: Build for Windows

on:
  push:
    tags:
      - '*'

jobs:
  build:
    name: Build on Windows
    runs-on: windows-latest

    steps:
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '>=1.24'

      - name: Check out code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get dependencies
        run: go get -v ./...

      - name: Build
        env:
          GOOS: windows
          GOARCH: amd64
          CGO_ENABLED: 0
        run: go build -v -o seraph-api-merger-windows.exe ./main.go

      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          files: seraph-api-merger-windows.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
